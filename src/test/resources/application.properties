# Java Mail Settings
spring.mail.host=smtp.gmail.com
spring.mail.port=25
spring.mail.username=${MAIL_ENDERECO}
spring.mail.password=${MAIL_SENHA}
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.enable=false
spring.mail.properties.mail.smtp.auth=true

# Sweep
sweepService.dataBasesPath=${DATABASES_PATH}
sweepService.gfixPath=${GFIX_PATH}
sweepService.dbaNome=${DBA_NOME}
sweepService.dbaUser=${DBA_USER}
sweepService.dbaSenha=${DBA_SENHA}
sweepService.destinatarios=${DESTINATARIOS_NOTIFICACAO_SWEEP}